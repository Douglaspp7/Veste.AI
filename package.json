{
  "name": "veste-ai",
  "version": "0.1.0",
  "description": "Provador virtual com IA — experimente roupas em tempo real pelo navegador",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
